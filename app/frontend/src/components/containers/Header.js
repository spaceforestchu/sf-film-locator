import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <nav className="header">
        <h1 className="header__title">SF Film Map</h1>
      </nav>
    );
  }
}

export default Header;
